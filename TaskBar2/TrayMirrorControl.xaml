<UserControl x:Class="TaskBar2.TrayMirrorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="112">
    <Border Background="#252525"
            Padding="6,0">
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center">
            <ItemsControl ItemsSource="{Binding TrayItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="{Binding DataContext.TrayButtonWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Height="{Binding DataContext.TrayButtonHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Padding="0"
                                Margin="0,0,1,0"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                ToolTip="{Binding ToolTip}"
                                Click="TrayIcon_Click"
                                MouseDoubleClick="TrayIcon_DoubleClick"
                                MouseRightButtonUp="TrayIcon_RightClick">
                            <Grid>
                                <Image Source="{Binding Icon}"
                                       Width="{Binding DataContext.TrayIconSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       Height="{Binding DataContext.TrayIconSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding ClockText}"
                       MinWidth="54"
                       Margin="8,0,0,0"
                       Foreground="{StaticResource TaskbarTextBrush}"
                       FontSize="{Binding ClockFontSize}"
                       TextAlignment="Center"
                       VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
